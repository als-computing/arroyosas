export default function Snapshot({rawArray}) {
    //this function allows the user to take two snapshots of array data.
    //when user clicks the first button, the current raw array data is stored in a ref.
    //when user clicks the second button, the current raw array data is stored in a ref.
    //after the user has clicked each button, the corresponding ref array data is sent into
    //plotlyheatmap.jsx to be displayed.

    //the user can then closely compare the data without having to keep the websocket running
    return (
        <div>

        </div>
    )
}
